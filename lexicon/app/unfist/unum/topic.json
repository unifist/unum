{
  "lexicon": 1,
  "id": "app.unifist.unum.topic",
  "revision": 0,
  "description": "Stores topics for an Unum, the core of an Unum",
  "defs": {
    "main": {
      "type": "record",
      "key": "nsid",
      "record": {
        "type": "object",
        "required": ["slug", "description"],
        "properties": {
          "slug": {
            "description": "Computer readable id",
            "type": "string",
            "format": "nsid"
          },
          "description": {
            "type": "string",
            "maxGraphemes": 256,
            "maxLength": 1024
          }
        }
      }
    }
  }
}
