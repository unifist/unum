{
  "lexicon": 1,
  "id": "app.unifist.unum.goal",
  "revision": 0,
  "description": "Stores goals for an Unum, the purpose of an Unum",
  "defs": {
    "main": {
      "type": "record",
      "key": "nsid",
      "record": {
        "type": "object",
        "required": ["aspect", "change", "description"],
        "properties": {
          "aspect": {
            "type": "string",
            "maxLength": "32"
          },
          "change": {
            "type": "string",
            "maxLength": "32"
          },
          "description": {
            "status": {
              "type": "string",
              "maxGraphemes": 256,
              "maxLength": 1024
            }
          }
        }
      }
    }
  }
}
